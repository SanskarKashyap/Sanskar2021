#include <stdio.h>
int main()
{
    int a, b;

    printf("Enter 2 Diffrent Number For Swaping \n");
    scanf("%d%d", &a, &b);
    printf("Before a = %d , b = %d", a, b);
    // int c=a;
    // int a=b,b=c ;

    printf("after a = %d , b = %d", b,a);
    return 0;
}