#include <stdio.h>
#include <math.h>
int main()
{
    int a, b,rem,ln;
    printf("Enter 5 digit number ");
    scanf("%d", &a);
    while (a>0)
    {
        rem = 
    }
    

    while (rem > 0)
    {
        rem = a % 10;
        printf("%d\n", rem);
        a = a / 10;
    }
   for (int i = 5; i > 0; i--)
   {
      int b= a/(int)pow(10,a);

   }
   

    // 1234
    // 1234 /1000 = 1

    return 0;
}